	общая ширина колонки: width+padding+border

	общий размер макета берется по крайним направляющим:
	width+padding+border
	необходимо давать какое-то кол-во px в PADDINGи, для того что бы контент не прилип(на каких-нибудь размерах)к краям браузера

	центровщик: margin:0 auto;
	центровать элемент:margin-left: auto; margin-right: auto;
	если через float делать сетку то лучше сводить все в одну сторону. float-left + margin-right и  у последнего элемента margin-right=0
	если через float делать сетку и не сводить в одну сторону то необходимо оставлять запас между floatными элементами

	стоит брать "запас" для длинного блока где оин край без контента

	clearfix надо вешать на непосредственного родителя floatных элементов
	блоки с логотипами можно задавать фиксированную высоту

	пересмотреть все ширины мелких блоков
	лучше засунуть все флотные элементы в отдельные блоки-clearfix

	иногда стоит соблюдать единообразие горизонтальных размеров блоков в макете

	высота строки-высота шрифта=.../2=.... - столько нужно добавить снизу и сверху от края буквы для того что бы нормально высчитывать отступы снизу и сверху если нету указания высоты строки то стоит увеличивать высоту шрифта на 30%

	если делать сетку через inline-block то необходимо сразууказывать vertical-align:top; ( по умолчанию baseline)
	если необходимо состыковать инлайн блоки впритык то стоит:
	родителю указывать размер шрифта=0; и дочерним элементам явно прописывать размер шрифта.

	position relative используется в связке с absolute(родитель -relative ,дочерний -absolute) а так же для наслаивания с z-index(z-index со свойством static,по умолчанию, не работает)
	position relative оставляет место за элементом в потоке,а если сдвигать,то сместится "копия",и останется пустое место влияющее на поток

	position absolute выпадают из потока полностью

